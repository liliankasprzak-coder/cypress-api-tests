name: CI - API tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: api-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install
        run: npm ci

      - name: Run Cypress (API) - Smoke Test
        uses: cypress-io/github-action@v6
        with:
          spec: cypress/e2e/exercicio1_APIs_tests/1-get-posts.cy.js
          browser: chrome

      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-artifacts-api
          path: |
            cypress/screenshots
            cypress/videos
          if-no-files-found: ignore
